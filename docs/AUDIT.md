# 稽核紀錄

| 日期 | 版本 | 變更 | 風險狀態 |
|---|---|---|---|
| 2026-09-22 | v1.0 | 初版:12 積木架構、工號登入、預約/審核/簽收/歸還/盤點、每日提醒;移除瀏覽器示範模式(前端不含任何業務規則與資料) | 已知風險見 SECURITY.md |
| 2026-09-23 | v1.1 | 套用 E Ink 企業識別色(Pantone 186 Red #C8102E / 432 Gray #333F48)與新版 UI;效能:後端依路由選擇性載入工作表 + CacheService 版本快取,前端讀取快取先畫再背景更新,盤點頁由 N+1 次請求降為 2 次;修正 isDate 未檢查日曆(2026-13-01 曾被接受);新增 docs/FLOW.md | 無新增對外介面;快取以 DBVER 版本失效,寫入即 bump |
| 2026-09-23 | v1.2 | 讀取再切細到欄位層級與「只讀未結案的列」(記憶積木支援 `{ cols, only }`);管理者可切換「同仁視角」預覽;e2e 對全部讀取動作做限縮 vs 全載一致性比對,突變測試增至 17 項 | 限縮讀取由測試把關,無新增對外介面 |

| 2026-09-23 | v1.3 | 展品改用可維護的分類清單(新增「分類」工作表,預設 eReader / eNote / Logistics & Factory / Prism / Signage / Lifestyle / Mobile & Wearables);目錄與展品管理依分類分段 + 籤條;管理者可新增、改名、調順序、停用分類;突變測試增至 20 項 | 改名會連動展品;有展品的分類不可停用 |

| 2026-09-23 | v1.4 | 盤點頁加強庫存對比:依分類分段 + 籤條、上方總計對照(已盤/應在庫/實點/差異/借出中)、逐台品項即時顯示差異、借出中的單台與借用單另外列出(在誰手上、哪天還) | 純前端改動,後端未動,不需重新部署 Apps Script |

| 2026-09-23 | v1.5 | 借用單流程優化:待審核可改單(updateLoan)、申請延期與轉借(requestExtend / requestTransfer / decideRequest / extendLoan,走同一套 request + 當面確認)、批次核准(approveMany)、點交與簽收自動指派單台、借用單列印、總覽「今天要做的事」、目錄多選一起填單 | 延期會檢查延長期間的庫存;批次核准逐張獨立、失敗不影響其他張;突變測試增至 25 項 |

| 2026-09-23 | v1.6 | 借用單頁提速:五個進行中分頁共用一次請求(前端切換不打後端)、路由 `tables` 可依參數決定讀取範圍(進行中只讀未結案的列,少讀 50% 儲存格)、前端快取 15 秒新鮮度窗口、規則層以 WeakMap 建立每次請求的總數與佔用索引 | 索引隨 dirty() 失效並有突變測試把關;規則層仍未改動資料本身(rules 測試通過) |

| 2026-09-23 | v1.7 | 展品編輯:新增「刪除展品」(只有沒被借過的才可刪,連帶刪單台編號;借過的擋下並要求改用下架)、照片直接從手機/電腦上傳(新增檔案積木 `60_files.gs`,前端縮圖後存進雲端硬碟「展品照片」資料夾,試算表只存連結)、存放位置改為下拉選單(預設新竹/林口,可自行新增) | **需要重新授權雲端硬碟權限**(編輯器執行一次 `authorizeDrive`);上傳路由以 `route.big` 單獨放寬字串長度,其他路由維持 500 字;突變測試增至 30 項 |

| 2026-09-23 | v1.7.1 | 修正快取賽跑:寫入(刪除/儲存)發生時,先前還在路上的讀取回來後會把寫入前的舊資料寫回快取並重畫,造成剛刪掉的展品還留在清單上。改為每次寫入把快取「世代」+1 並清掉在途請求,舊世代的回應一律丟棄並重抓 | 純前端,後端未動,不需重新部署 Apps Script;UI 測試新增賽跑回歸案例(刻意把讀取回應壓到刪除之後) |

| 2026-09-23 | v1.8 | 存放地點變成一等公民:同一個展品可以分散在多個廠區(展品表新增 `stock` 欄,記各地點數量與各自的盤點日;舊資料自動視為全部放在原本那個地點)。借用單每一行帶地點、可借量與在庫分地點各算各的、點交只能指派該地點的單台、歸還可以選還到哪一區(等於庫存搬廠)、盤點可以只盤一個廠區 | 舊資料相容由 e2e 守著;突變測試增至 37 項(新增 6 項分地點守門);**需要重新部署 Apps Script** |

| 2026-09-23 | v1.8.1 | 修正「資料全不見了」:GAS 的 /exec 會先回 302 再轉址,剛換版本那幾秒 POST 偶爾會被當成 GET 重送,回來的是 `doGet` 的健康檢查頁;舊版的 doGet 長得跟正常回應一樣(`success:true` + 一串字),前端照單全收存進快取,畫面就變成清單全空、`S.items.filter is not a function`。doGet 改為 `success:false, health:true`,連線積木看到 health(或舊版那串「API 運作中」)就當成連線失敗:讀取類自動重試一次,寫入類明確報錯(那次寫入其實沒發生) | 資料本身沒有受影響(只是前端快取被汙染,重新整理即可);UI 測試加上回歸案例(假裝第一次 catalog 回健康檢查頁),突變測試增至 38 項 |

| 2026-09-23 | v1.8.2 | 建置時把內容雜湊掛到 index.html 的 js/css 上(`?v=<8 碼>`)。之前修好並部署後,瀏覽器還會繼續用快取裡的舊 js 好幾分鐘,使用者看到的仍是壞掉的畫面(修 v1.8.1 時就是這樣) | 內容沒變雜湊就不變,不會白白重抓;build.js 會自我檢查有沒有掛上去,結構測試也守著 |

| 2026-09-23 | v1.9 | 防資料遺失三道保險:① `sheet_()` 找不到工作表直接報錯,**不再自動建立+塞預設分類**(建立與種子只留在人工執行的 `setupSheets`)② 每次讀取驗標題列的關鍵欄位,對不上就停(擋「開到別的試算表」)③ `load()` 記錄每張表是否完整載入,`save()` 寫回前先全部檢查,只讀了部分欄位或部分列就拒絕整張寫回。另附每日備份 `dailyBackup` / `installBackupTrigger`(預設不啟用) | 這三種錯誤標為 userFacing,訊息會原樣顯示並提醒「不要重新初始化」;訊息不含試算表 ID。突變測試增至 41 項(新增 3 項)。**部署前務必確認六張工作表都在** |

| 2026-09-23 | v2.0 | 展覽檔期:新增「展覽」工作表 + 借用單的 `showId`;**殘額佔位**(展覽佔用量 = max(0, 規劃量 − 已開單量))避免展覽卡位與借用單重複扣同一批庫存;整批貼上、依地點產生借用單、批次延期(`extendMany`);新增只補新表的 `upgradeSheets()` | 重複扣庫存是這個功能最容易靜靜出錯的地方,規則 11 條 + e2e + 突變 8 條 + UI 一條在守;**新增工作表的版本部署順序不可反**(先貼程式存檔 → 執行 upgradeSheets → 才建立新版本) |

| 2026-09-23 | v2.1 | 展覽流程改成「先開單定檔期、再去目錄挑展品」;目錄新增挑選模式(`catalog` 接 `showId`,排除自己那場的卡位);挑選狀態存 localStorage;同仁那個購物車分頁改名為「借用申請」 | 存檔前先向後端要一次現況,免得記憶體裡沒有的場地 / 承辦人被空值洗掉 |

| 2026-09-23 | v2.2 | 全流程審查:修 8 個會弄錯資料的後端 bug(同一台重複歸還、跨地點誤歸還、駁回沒清請求、展覽卡位基準不一致、結案展覽仍可掛單、下架不擋待審核、請求被覆蓋、讀取規格漏欄)+ 6 個前端流程 / 狀態 bug(登出不清工作狀態、存檔只清一半的挑選狀態、`S.editing` 沒持久化、分頁守衛失效、`S.showId` 殘留、挑選模式孤兒)+ 清死碼 | 每一條都補了回歸測試;最嚴重的三條(展覽重複扣、登出隔離、展覽清單被洗掉)實際把修正拿掉驗證過會紅。規則 21 → 27,突變 49 → 57 |

| 2026-09-24 | v2.3 | 展後結算(規劃 / 實際借出 / 已歸還 / 短少 / 未歸還 五欄對照 + 匯出 CSV)、整場批次申請歸還(`returnMany`)、已結案借用單封存到「借用單歷史」工作表(`archivePreview` / `archiveLoans`),借用單頁新增「含歷史資料」勾選 | **封存是同時動兩張表的高風險動作**:先附加到歷史表(`appendHist` + `flush`),確定成功才從借用單表移除;歷史表只能附加,`save()` 擋掉整張寫回;中斷後重跑會跳過已搬過的單。結案時把結算存成快照,單被搬走後數字仍在;已封存的展覽不准重開、不准刪。規則 27 → 33,突變 57 → 71,結構 10 → 12 |

| 2026-09-24 | v2.3.1 | 清單的「移除這一項」改成垃圾桶圖示 + 文字 + 外框、固定靠右、hover 轉紅 | 功能本來就在也是好的,但只是個灰色 ✕ 貼在「在庫 3」旁邊,使用者找不到只好整張清空。找不到的東西等於不存在,當成真的 bug 修 |

| 2026-09-24 | v2.3.2 | 換版偵測:建置編號填進 `<meta name="build">` + 不進快取的 `version.json`;載入時與分頁回前景時(最多十分鐘一次)比對,不一樣就掛「立即更新」橫幅 | `?v=` 只保護得了 js/css,index.html 自己被快取住時裡面寫的還是舊的 `?v=`。**不自動重整**(有人可能正在填單);更新用 `location.replace` 換網址,不用 `reload()`(可能又拿到快取裡同一份舊 HTML)。結構 12 → 13 |

| 2026-09-24 | v2.4 | ① 部分開單:每行取 min(還要借的量, 真的借得到的量),缺的留在需求清單上繼續卡位,進貨後再按一次補開 ② `holders` 路由:缺口點得開,列出是哪幾張借用單(依歸還日排序)與哪幾場展覽卡著 ③ `showSheet` 路由:依廠區分頁的備料清單(可列印、有勾選格)+ 整場一張的 CSV | 刻意不做「硬開超額」—— 帳面 12 台實際 10 台的單,點交一定對不起來。同仁看得到數量與歸還日,看不到借用人姓名。總清單規劃中就出得來(備料發生在開單之前)。規則 33 → 38,突變 71 → 77 |

| 2026-09-24 | v2.5 | 速度:① 沒有先後關係的請求一起發(借用申請、展覽明細、目錄、展品管理)② 讀取快取存進 localStorage,跨重整活下來 ③ 登入後預抓(順便叫醒冷掉的容器)④ 寫入逾時 30 → 75 秒 | **實測後推翻了原本的方向**:`status`(幾乎不讀資料)1,729 ms vs `loans all`(讀整張表)1,742 ms —— 每趟 1.2~1.8 秒是固定成本,與讀取量無關,偶爾跳到 8~26 秒,冷啟動 45.7 秒。所以「只讀需要的欄位」這條路已經走到底,改的是趟數與乾等。④ 是資料正確性不是速度:冷啟動 45 秒 > 原本 30 秒逾時,瀏覽器報逾時但伺服器可能已經寫進去了。結構 13 → 15 |

| 2026-09-25 | v2.5.3 | ① **離譜的借出 / 歸還日期不再送得進來**:規則層新增 `saneDate` / `saneRange`(往前 3 年、往後 5 年、單一期間最長 2 年),借用單、修改申請、延期、批次延期、展覽檔期、需求試算、`holders`、目錄期間篩選全部走同一套;前端同步一份上下界,日期欄位掛 `min`/`max`,摘要列直接說「日期有問題」並鎖住送出鈕 ② 展品目錄的分類籤條釘在標題列底下 ③ 目錄搜尋旁那顆只剩圖示的按鈕補上 `title` / `aria-label`(它是「匯出庫存 CSV」) | `2026-02-30` 早就擋得掉,擋不掉的是**日曆上合法但離譜**的年份:`0025-10-01`、`9999-12-31` 一路寫進工作表,之後逾期天數、可借量、結算數字全被那張單帶歪,而且很難看出是哪一張。前端只是先擋一次省一趟來回,**真正的守門在規則層**,拿掉任一道 e2e 都會紅(突變 77 → 81)。籤條的 `z-index` 必須低於標題列(15 < 20),`--toph` 由 `measureTop()` 量出來,不寫死高度。規則 38 → 40,結構 15 → 16(新增「前後端日期上下界必須一致」) |

## 部署基準(每次部署後補上)
| 日期 | Apps Script 版本 | Pages commit | 測試結果 |
|---|---|---|---|
| 2026-09-22 | 1 版(v1.0 12積木架構首次上線) | 57c0aa6 | 規則 8 / e2e 全過 / 結構 8 / 突變 10 |
| 2026-09-23 | 2 版(v1.1 選擇性載入工作表+快取,單台一次查詢) | 775c7d1 | 規則 8 / e2e 全過(含限縮載入一致性)/ 結構 8 / 突變 10 / UI E2E 通過 |
| 2026-09-23 | 3 版(v1.2 欄位層級讀取 + 未結案尾段讀取) | 346118f | 規則 8 / e2e 全過 / 結構 8 / 突變 17 / UI E2E 通過 |

| 2026-09-23 | 4 版(v1.3 分類清單) | 97d2d61 | 規則 8 / e2e 全過 / 結構 8 / 突變 20 / UI E2E 通過 |

| 2026-09-23 | (不變,仍為 4 版) | 21179d8 | 規則 8 / e2e 全過 / 結構 8 / 突變 20 / UI E2E 通過 |

| 2026-09-23 | 5 版(v1.5 借用單流程) | b01e743 | 規則 8 / e2e 全過 / 結構 8 / 突變 25 / UI E2E 通過 |

| 2026-09-23 | 6 版(v1.6 借用單提速) | 9e7b8b2 | 規則 8 / e2e 全過 / 結構 8 / 突變 26 / UI E2E 通過 |

| 2026-09-23 | 7 版(v1.7 刪除 + 照片上傳 + 地點下拉) | 38aa1ea | 規則 8 / e2e 全過 / 結構 8 / 突變 30 / UI E2E 通過 |
| 2026-09-23 | (不變,仍為 7 版) | (v1.7.1 快取賽跑修正) | 規則 8 / e2e 全過 / 結構 8 / 突變 30 / UI E2E 通過 |
| 2026-09-23 | 8 版(v1.8 分地點庫存) | 9f348de | 規則 10 / e2e 全過 / 結構 8 / 突變 37 / UI E2E 通過 |
| 2026-09-23 | 9 版(v1.8.1 健康檢查頁要認得出來) | 01c6ca1 | 規則 10 / e2e 全過 / 結構 9 / 突變 38 / UI E2E 通過 |
| 2026-09-23 | (不變,仍為 9 版) | (v1.8.2 前端快取破壞) | 規則 10 / e2e 全過 / 結構 10 / 突變 38 / UI E2E 通過 |
| 2026-09-23 | 10 版(v1.9 防資料遺失三道保險) | 177b6ec | 規則 10 / e2e 全過 / 結構 10 / 突變 41 / UI E2E 通過 |
| 2026-09-23 | 11 版(v2.0 展覽檔期) | 356b511 | 規則 21 / e2e 全過 / 結構 10 / 突變 49 / UI E2E 通過 |
| 2026-09-23 | 12 版(v2.1 展覽流程:先定檔期再挑展品) | 212aa7e | 規則 21 / e2e 全過 / 結構 10 / 突變 49 / UI E2E 通過 |
| 2026-09-23 | 13 版(v2.2 全流程審查) | e3361aa | 規則 27 / e2e 全過 / 結構 10 / 突變 57 / UI E2E 通過 |
| 2026-09-24 | 14 版(v2.3 展後結算 + 批次申請歸還 + 借用單歷史) | 0d35d3a | 規則 33 / e2e 全過 / 結構 12 / 突變 71 / UI E2E 通過 |
| 2026-09-24 | (不變,仍為 14 版) | d1076c7(v2.3.1 移除鈕看得出來) | 規則 33 / e2e 全過 / 結構 12 / 突變 71 / UI E2E 通過 |
| 2026-09-24 | (不變,仍為 14 版) | 242dd93(v2.3.2 換版偵測) | 規則 33 / e2e 全過 / 結構 13 / 突變 71 / UI E2E 通過 |
| 2026-09-24 | 15 版(v2.4 部分開單 + 缺口來源 + 備料清單) | 8b9fc37 | 規則 38 / e2e 全過 / 結構 13 / 突變 77 / UI E2E 通過 |
| 2026-09-24 | (不變,仍為 15 版) | d31c8a8(v2.5 速度) | 規則 38 / e2e 全過 / 結構 15 / 突變 77 / UI E2E 通過 |

## 環境
- Apps Script 專案:展品管理後端 `1Mk2JuR7fYYj8bl1w1hh1o2ICCsFalu1u9xsc3_Op68DwpM1vuNFzNrZY`
- 網頁應用程式部署 ID:`AKfycbyKSJTXKFkZPx0ub45aKd7_0kn3Gv3379GYRJAnE9lfhXpR60HlhtWKSdgrebIyZTQssQ`(換版本不換網址;`...WKSdgreb` 後面是**大寫 I**,不要用眼睛抄,一律從 `deploy.config.json` 複製)
- 前端:GitHub Pages `p60732/eink-exhibits`,由 GitHub Actions 測試通過後發佈

## 待辦
- 目前沒有待辦。

## 已決定不做
- **每日逾期提醒排程**:`installDailyTrigger()` **不要執行**(使用者 2026-09-23 決定)。程式碼留在 `50_schedule.gs`,但跑了會建出「每日 08:30 逾期提醒」觸發器。
  ⚠️ 編輯器的函式下拉會停在上一次的選擇,曾因此誤執行兩次,建出來的觸發器都已刪除。按「執行」之前一定要去「執行項目」頁確認實際跑的是哪一個函式。
- 跨點調撥的獨立功能(v1.8 決定:要搬就直接編輯各地數量,或歸還時選還到別區)。
